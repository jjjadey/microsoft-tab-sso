{
    "name": "myTabSso",
    "version": "0.0.1",
    "description": "",
    "author": "",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "install:tabs": "cd tabs && npm install",
        "install:server": "cd server && npm install",
        "installAll": "npm run install:tabs && npm run install:server"
    },
    "devDependencies": {
        "@microsoft/teamsfx-cli": "0.*"
    },
    "license": "MIT"
}
